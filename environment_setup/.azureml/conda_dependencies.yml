name: iris_demo
channels:
  - defaults
  - conda-forge
dependencies:
  # The python interpreter version.
  # Currently Azure ML Workbench only supports 3.5.2 and later.
  - python
  - pip
  - numpy
  - pandas
  - pytest
  - pip:
      - azureml-sdk
      - black
      - cached-property
      - jsonlines
      - nteract-scrapbook
      - pydocumentdb
      - tqdm
      - pyemd
      - ipywebrtc
      - pre-commit
      - scipy
      - scikit-learn
      - requests
      - requests-oauthlib
      - regex
      - seaborn
      - joblib
